import styled from 'styled-components';

const TopBar = styled.div`
    max-width: 1420px;
    padding: 24px 42px 0 42px;
    margin-bottom: 32px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
`;

const TitleLogo = styled.img`
`;

export { TopBar, TitleLogo };
